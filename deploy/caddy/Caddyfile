# Simpler alternative to nginx: automatic TLS, no certbot.
# Caddy sets X-Forwarded-For to the true peer by default.
api.example.com {
    request_body {
        max_size 256KB
    }
    reverse_proxy 127.0.0.1:8080
}
